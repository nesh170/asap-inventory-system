language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests, the migrate doesn't really do anything right now, change it to the test one when tests are written
script:
  - python manage.py migrate #remove this in future
 #- python manage.py test